# inventory-movements
    庫存異動表頭；記錄庫存領域、異動類型、來源／目的地點、負責員工與過帳狀態。
* store_inventory_movement_items  
    商城庫存異動明細關聯表；記錄商城商品規格及異動當下的 SKU、品名快照。
* rental_inventory_movement_items  
    租借庫存異動明細關聯表；記錄租借 SKU 規格及異動當下的 SKU、品名快照。

## 舊 Mock 搬移結論（2026-07-22）

`frontend/data/admin/movement.json` 目前保留為 Mock，Seed 不建立 `inventory_movements`，原因是來源不足以滿足本文件的 variant、表頭與員工 FK 約束：

- 100 張異動、141 筆明細全部只有 `productId`，沒有 variantId；其中 24 筆屬於多規格商品，無法唯一對到 `product_variants`。
- 26 張異動在同一表頭混合不同 type／from／to，不能對到單一 `inventory_domain + movement_type + locations` 表頭。
- `employeeId` 只有 01、02、03，沒有可追溯的 `admin_users` 主檔對照。
- 「調撥」為商城→租借跨領域異動，必須拆為 `conversion_out` 與 `conversion_in`；舊 JSON 並沒有共用轉換單號或 canonical 租借 variant。

後續最低補件為：每筆明細的 canonical variantId，每張單唯一的領域／type／來源／目的，01～03 的 `admin_users.id` 對照，以及跨領域調撥的配對 ID。資料齊備前不得用單一規格或開發管理員代填。


## 關聯與資料流
inventory_movements
  ├─ store_inventory_movement_items
  └─ rental_inventory_movement_items

### 關聯
* inventory_movements：異動單表頭，為商城與租借共用
* store_inventory_movement_items：商城異動明細；透過inventory_domain 驗證
* rental_inventory_movement_items：租借異動明細；透過inventory_domain 驗證

### 資料流程
建立庫存異動時：
1. 在 `inventory_movements` 建立 `draft` 表頭，指定庫存領域、異動類型、來源或目的地點、員工與原因。
2. 商城異動明細寫入 `store_inventory_movement_items`；租借異動明細寫入 `rental_inventory_movement_items`。
3. 明細的 `inventory_domain` 必須與表頭相同，資料庫以複合外鍵強制驗證。
4. 異動類型決定表頭地點組合：進貨只允許目的地、損耗只允許來源地、移轉必須同時有不同的來源與目的地；**`product_stock_update` 表頭兩端皆 NULL**，庫位寫在明細列。
5. 表頭改為 `posted` 時必須填入 `posted_at`；`draft` 與 `cancelled` 不得填入 `posted_at`。
6. **ADM-W2-08**：`product_stock_update` 的 post **只定稿、不改** `inventory_stocks`（商城 on-hand 由 Admin Products 寫入）。`conversion_*` 成對過帳仍改兩邊庫存。
7. 讀取清單或明細時，使用 `inventory_movement_items_view` 或 `inventory_movement_dto_view` 將兩個領域的資料統一呈現。


## 欄位說明
### inventory_movements
* id                            異動流水號
                                `GENERATED BY DEFAULT AS IDENTITY`。

* movement_no                   異動單號，UNIQUE。
* inventory_domain              庫存領域，只允許 `store` 或 `rental`。

* movement_type                 異動類型，不可為空白。
                                `receipt`
                                `write_off`
                                `transfer`
                                `conversion_out`
                                `conversion_in`
                                `product_stock_update`  （ADM-W2-08：商品 Modal 稽核單；表頭 source／destination 必須皆 NULL；細節在明細列）

* status                        異動狀態，只允許 `draft`、`posted`、`cancelled`。

* source_location_id            來源庫存地點，NULL；
                                與 `inventory_domain` 組合後外鍵指向 `inventory_locations`。
                                `product_stock_update` 表頭必須為 NULL（列級庫位在明細）。

* destination_location_id       目的庫存地點，NULL；
                                與 `inventory_domain` 組合後外鍵指向 `inventory_locations`。
                                `product_stock_update` 表頭必須為 NULL。

* employee_id                   負責員工，外鍵指向 `admin_users.id`。
                                *idx_inventory_movements_employee ON (employee_id)*

* reason                        異動原因，不可為空白。
* occurred_at                   異動發生時間，由呼叫端提供。
* posted_at                     過帳時間，NULL；只有 `status = posted` 時必填。
* created_at                    建立時間，預設 `now()`。
* updated_at                    最後更新時間，預設 `now()`；DDL 未定義自動更新 Trigger。
*UNIQUE(id, inventory_domain)*
*idx_inventory_movements_domain_status_time ON (inventory_domain、status、occurred_at)*
*idx_inventory_movements_source_domain ON (source_location_id、inventory_domain)*
*idx_inventory_movements_destination_domain ON (destination_location_id、inventory_domain)*

### store_inventory_movement_items
* id                    明細流水號
                        `GENERATED BY DEFAULT AS IDENTITY`

* movement_id           異動表頭識別碼；
                        與 `inventory_domain` 複合外鍵指向 `inventory_movements`。(驗證是否為store)

* inventory_domain      固定為 `store`，預設 `store`。

* variant_id            商城商品規格識別碼，外鍵指向 `product_variants.id`。
                        *idx_store_inventory_movement_items_variant ON (variant_id)*

* sku_snapshot          異動當下 SKU 快照
* item_name_snapshot    異動當下品名快照
* quantity              異動數量，必須大於 `0`（正整數；方向靠 from／to）。
* source_location_id    列級來源庫位，NULL 表示 UI「---」；與 `inventory_domain` 外鍵指向 `inventory_locations`。
* destination_location_id 列級目的庫位，NULL 表示 UI「---」。
* line_reason           列級備註，可空（選填）。API／契約欄位名 `lineReason`；Admin UI 標籤為「備註」。
* line_nature           列級異動性質，可空。API `lineNature`；白名單：
                        `receipt`（進貨）／`transfer`（移轉）／`stocktake`（盤點）／
                        `damage`（折損）／`write_off`（損耗）。
                        與 from／to **獨立**：改性質不改庫位；產單時前端依 from／to 推導預設後可再改。
* CHECK：至少一端庫位非 NULL；兩端都有時必須不同。
* CHECK：`line_nature` 為 NULL 或上列白名單之一。
*idx_store_inventory_movement_items_movement ON (movement_id、inventory_domain)*

### rental_inventory_movement_items
* id                    明細流水號
                        `GENERATED BY DEFAULT AS IDENTITY`。

* movement_id           異動表頭識別碼；
                        與 `inventory_domain` 複合外鍵指向 `inventory_movements`。(驗證是否為rental)

* inventory_domain      固定為 `rental`，預設 `rental`。

* rental_sku_variant_id 租借 SKU 規格識別碼
                        *idx_rental_inventory_movement_items_variant ON (rental_sku_variant_id)*

* sku_snapshot          異動當下 SKU 快照
* item_name_snapshot    異動當下品名快照
* quantity              異動數量，必須大於 `0`。
*idx_rental_inventory_movement_items_movement ON (movement_id、inventory_domain)*



## 運作模式
* 異動單表頭 > `inventory_movements`
* 商城異動明細 > `store_inventory_movement_items`
* 租借異動明細 > `rental_inventory_movement_items`
* 統一明細讀取 > `inventory_movement_items_view`
* 後台／報表 JSON DTO > `inventory_movement_dto_view`

### 領域與狀態
* 一張異動單不能同時包含商城與租借明細。
* 明細中的 SKU 與品名是異動快照，不應在商品主檔更名後回寫。



## 程式碼追蹤
* 前端異動紀錄
`admin/partials/movement.html`
        ↓
`admin/js/movement.js`
        ↓
`loadAdminJsonResource()`
        ↓
`AdminAPI.movement.list()` 或 `data/admin/movement.json`
        ↓
`window.movementCache`
        ↓
篩選、排序、表格與明細 Modal

* `admin/js/movement.js` 第 64–76 行優先使用 `AdminAPI.movement.list()`；未啟用後端時讀取 `data/admin/movement.json`。
* `admin/js/movement.js` 第 168–174 行將新增紀錄放入 `window.movementCache`，並呼叫 `AdminAPI.movement.create()`。
* `admin/js/admin-api.js` 第 214–220 行預留 `GET`／`POST /api/admin/inventory-movements`；預設 mock 模式不會發出 HTTP 請求。
* `admin/js/movement.js` 第 1–4 行指明正式後端應以 `inventory_movement_items_view` 組成 DTO；現行 JSON 的欄位資料並非直接讀取三張正式表。
* `V752__normalize_inventory_movement_types.sql` 將舊有中文與調整類型映射為 `receipt`、`write_off`、`transfer`；正式 API 必須輸出 canonical 值，並由前端顯示層轉換為中文標籤。

### 前端調撥建立異動
`admin/partials/products.html`
        ↓
`admin/js/products.js`
        ↓
`pendingMovementItems`／`pendingRentalMovementItems`
        ↓
`window.addMovementRecord()`
        ↓
前端快取與 mock API

* `admin/js/products.js` 第 5901–5906 行在調撥後組成 `pendingMovementItems`。
* `admin/js/products.js` 第 1393–1399 行將待處理明細組成前端異動記錄；目前是 UI／快取模型，沒有轉換為 `inventory_movements` 及兩張明細表的寫入命令。



## G-3／W2-08 實作狀態

* **ADM-W2-08（契約 v0.16／v0.17；✅ 已驗收）**：新單以 `product_stock_update` 為主；明細可帶列級 from／to／`line_reason`／`line_nature`；**post 不定庫存**。異動頁改唯讀；商城 on-hand 由 Products 寫入。
* **方案 B（v0.17）**：列表仍不顯示異動性質欄；詳情列性質可下拉改並寫入 `line_nature`；產單帶 from／to 推導預設（進貨／移轉／損耗）；盤點／折損僅手動。`line_reason` UI 稱「備註」。
* 歷史 `receipt`／`write_off`／`transfer` 仍可讀；開發環境不符格式之舊異動資料可清（Seed 本來就不建 movements）。
* 跨領域 `conversion_out`／`conversion_in` 由 `/api/admin/inventory-conversions` 成對處理；**過帳仍改**兩邊 on-hand；只允許 store→rental。
* 現有表頭只有一個 `employee_id`，用來保存最後過帳或作廢操作者；改 reason 的 PATCH **不**更新 `employee_id`。
* 中風險：三張表的 `updated_at` 只有預設值，更新時不會自動刷新。
